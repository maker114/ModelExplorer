# 更新日志

本文件记录 Model Explorer 面向用户的重要变更，只写值得用户知道的内容，不是 git 日志的堆砌。

格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/0.3.0/)；
版本号遵循仓库 `AGENTS.md` 的 `主.次.修订` 口径，真源为 `ModelExplorer/AssemblyInfo.cs`。

## [Unreleased]

## [3.4.1] - 2026-09-13

### Added

- 新增 6 套配色预设，共 12 套：**石墨灰**（完全中性，配壁纸最百搭）、**深海青**、
  **樱花粉**、**靛蓝**、**赤陶橙**、**苔原绿**。

### Changed

- **毛玻璃材质改为无色**：面板、侧栏与悬停态的填充和描边都去掉色相、只保留明度
  （Rec.601 亮度）。此前填充用的是主题面板色，熔岩红这类主题会把整片侧栏染成红色半透明，
  压在壁纸上像蒙了一层色纸；磨砂玻璃本身不吸色，主题色只该出现在强调色、文字与状态色上。
  实测熔岩红 + 壁纸时，侧栏分区框与列表卡片的 R−B 由明显偏红变为 −1～−3（中性）。
- 关闭毛玻璃（或把透明度拉到 0）时仍是原来的主题实色画刷，观感与 V3.1.3 一致，不受影响。

## [3.4.0] - 2026-09-12

### Added

- 毛玻璃改由两个滑杆控制：**模糊**（0～60 px，同时作用于极光与自定义背景图）与
  **透明度**（0～100 %，0 = 面板不透明、100 = 只剩边框），取代原来的三档强度，
  拖动即时预览。旧配置的三档强度会自动换算成等价滑杆值，观感不变。

### Changed

- 背景图按显示器缩放出图：150% 缩放下，同一窗口的位图由 600×390 提到 1800×1170，
  壁纸不再被放大三次，实测细节（相邻像素差）提高约 1.9 倍。
- 设置里不再重复提供「壁纸模糊」：模糊统一由毛玻璃分区的滑杆控制，避免两个滑杆互相打架。
- 有背景图时不再叠加主题色极光：窗口保持透明、由壁纸决定颜色，不再被主题色相染黄/染绿。
- 适配分段按钮的未选中文字改用常规文字色，选中态在强调色底上压深色字。

### Fixed

- **适配按钮选中态文字看不清**：控件构造时赋的 `Foreground` 是本地值，优先级高于模板触发器，
  选中态的颜色一直被它盖掉（强调色底上压着灰字）。改为在模板里直接对 `ContentPresenter`
  设 `TextElement.Foreground`。
- **背景图分区标题下的分割线贴到了输入框上边框**：路径行缺少上边距，两行并成一条粗线。
- **工程名检查弹窗的列表、以及设置里下拉菜单的选项不再透出壁纸**：这两处文字密集，
  改回不透明面板色（弹出层内部高频能量 1.02，同屏正文 8.32）。

## [3.3.0] - 2026-09-12

### Added

- 自定义背景图：可在「设置 → 背景图」选一张本地图片当窗口背景，六个窗口共用同一张。
  支持四种适配方式（覆盖 / 填充 / 居中 / 拉伸）、壁纸模糊（0～40 px）与暗化（0～90 %）。
- 自动可读性下限：无论暗化设得多低，背景都会被自动压到「面板上的小字与直接压在背景上的
  说明文字都保持 4.5:1 以上对比度」的亮度；实测最坏情况（纯亮图 + 暗化 0）为 5.0～5.2:1。
- 设置窗口的背景图分区会**实时预览**：改选图、适配、模糊、暗化立刻重画背景层，
  点「保存」才写入配置，取消或直接关窗会还原。

### Changed

- 毛玻璃背景层由「挂在视觉树上的 `BlurEffect`」改成**一次性烘焙位图**（`Backdrop.cs`）：
  模糊、暗化、颗粒都在像素上算完并冻结，每帧只是贴一张位图，效果开销不再随模糊半径与
  光斑数量变化；背景图也因此几乎不增加绘制成本。
- 背景图解码按长边上限 2560 降采样，同一路径跨窗口只解码一份，内存可控。
- `config.json` 新增 `BackgroundImage` / `BackgroundFit` / `BackgroundBlur` / `BackgroundDarken`；
  旧配置缺字段时等于「不设背景图」，不会影响原有观感。

## [3.2.0] - 2026-09-12

### Added

- 应用内毛玻璃界面：窗口内新增一层「极光」背景并整体做一次高斯模糊，界面面板改为半透明
  玻璃填充，配极细颗粒与更亮的 1px 边框。界面不再是一块块纯色板，但仍保持深色基调。
- 设置窗口新增「毛玻璃」分区：**毛玻璃效果**开关与**轻柔 / 标准 / 浓郁**三档强度，
  随「保存」写入配置，保存后主窗口重建即时生效；关闭开关时强度下拉置灰。
- 生效范围覆盖主窗口、设置窗口与 6 个对话框（确认 / 重命名 / 详细统计 / 整理报告 / 工程名计划）。

### Changed

- `config.json` 新增 `Glass` 与 `GlassStrength`。两项刻意可空：旧配置没有这两个字段时按
  「开启 + 标准档」处理，避免升级后被静默关掉毛玻璃或掉到最低档；强度越界值夹到 0～2。
- 标题栏版本号改为从程序集信息读取，不再在 `MainWindow.xaml` 里另写一份。
- 主界面状态栏底色由写死的 `#0D1017` 改为跟随主题的侧栏色，才能与其它玻璃面板一起半透明。
